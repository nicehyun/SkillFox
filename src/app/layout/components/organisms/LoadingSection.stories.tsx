import { Meta, StoryObj } from "@storybook/react";
import LoadingSection from "./LoadingSection";

const meta: Meta = {
  title: "Layout/Organisms/LoadingSection",
  component: LoadingSection,
  tags: ["autodocs"],
};

export default meta;

export const Default: StoryObj<typeof meta> = {};
