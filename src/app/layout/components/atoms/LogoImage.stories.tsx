import { Meta, StoryObj } from "@storybook/react";
import LogoImage from "./LogoImage";

const meta: Meta = {
  title: "Layout/Atoms/LogoImage",
  component: LogoImage,
  tags: ["autodocs"],
};

export default meta;

export const Default: StoryObj<typeof meta> = {};
